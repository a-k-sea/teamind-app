<div class="container">
  <div class="card-status">
    <div >
      <h2 class="text-center">Team <%= @team.name %> dashboard</h2>
      <h3 class="text-center">check out your teammates profiles</h3>
      <div class="row justify-content-center">
        <div class="col-10">
      <% @team.users.each do |user|  %>

    <div class="card-status-infos d-flex justify-content-between align-items-center">

      <%= cl_image_tag user.valid_photo_url, class: "avatar-large", crop: :fill %>
       <p class="mb-0"><%= user.first_name %> <%= user.last_name %></p>
      <%= link_to "See details", "#", class: "btn btn-ghost" %>
    </div>
    <% end %>
  </div>
  </div>
    </div>


</div>
  <div class="container mt-5" id="confirm-buttons">
  <%= link_to "Back to teams", teams_path, id:"small-btn" %>
  </div>
</div>


